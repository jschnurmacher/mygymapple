<?xml version="1.0" encoding="UTF-8" ?>
<Grid xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:fontAwesome="clr-namespace:fontAwesome"
    x:Class="MyGym.AccountBackToTrans"
    VerticalOptions="Start"
    Padding="0,0,0,5"
    MinimumHeightRequest="32">
    <Grid.GestureRecognizers>
        <TapGestureRecognizer Tapped="BackToAccountTrans_Tapped" />
    </Grid.GestureRecognizers>
    <Grid.RowDefinitions>
        <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="16"/>
        <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <Image Grid.Column="0" HorizontalOptions="Start" VerticalOptions="Center">
        <Image.Source>
            <FontImageSource Glyph="{x:Static fontAwesome:FontAwesomeSolid.ArrowCircleLeft}" FontFamily="FontAwesomeSolid" Color="#0079bb"></FontImageSource>
        </Image.Source>
    </Image>
    <Label Grid.Column="1" Text="Back to Account Payments" TextColor="#0079bb" FontSize="15" FontAttributes="Bold" VerticalOptions="Center" />
</Grid>